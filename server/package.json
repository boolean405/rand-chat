{
  "name": "rand-chat-server",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Boolean",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build": "npm install --prefix ../client && npm run build --prefix ../client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.11.0",
    "redis": "^4.7.0",
    "socket.io": "^4.8.1"
  }
}
